import { PageElProps } from 'interfaces';
import React from 'react';

class PageElement extends React.Component<PageElProps> {
  constructor(props: PageElProps) {
    super(props);
  }
}

export default PageElement;
